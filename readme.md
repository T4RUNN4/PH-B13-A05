# What is the difference between var, let, and const?
All of them used to declare variable in Javascript. But variable declared using "const cannot be changed later. On the other hand, variables declared with "var" is function scoped and has features like hoisting while variables declared with "let" is block-scoped.

<br>
<br>

# What is the spread operator (...)?
Spead operator in Javascript speads array's element or object's property into indiviual element or key-value pair.

<br>
<br>

# What is the difference between map(), filter(), and forEach()?
"map" transforms each element into a new array with same length, "filter" selectes element into a new array that matches condition and "foreach" executes a function without returning anything.

<br>
<br>

# What is an arrow function?
Arrow function is ES6 features which provides a altrnative and easy way of declaring function.

<br>
<br>

# What are template literals?
Template literal is a ES6 features which allows to declare strings with backtick(``) that allows features like string interpolation.